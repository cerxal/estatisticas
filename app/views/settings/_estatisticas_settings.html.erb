<%- defaults = Redmine::Plugin::registered_plugins[:estatisticas].settings[:default] -%>

<p>
Debes crear un campo personalizado para o tipo de versión co nome que se configura nesta pantalla. Isto permitirá que o listado de versións mostre o tipo da versión </p>
</p>
<p>
<%= label_tag 'settings[tipo_version]', l(:label_setting_tipo_version) %>
<%= text_field_tag('settings[tipo_version]', @settings["tipo_version"] || defaults["tipo_version"]) %>
</p>
<p>
<%= label_tag 'settings[min_horas]', l(:label_setting_min_horas) %>
<%= text_field_tag('settings[min_horas]', @settings["min_horas"] || defaults["min_horas"]) %>
</p>
<p>
<%= label_tag 'settings[max_horas]', l(:label_setting_max_horas) %>
<%= text_field_tag('settings[max_horas]', @settings["max_horas"] || defaults["max_horas"]) %>
</p>
<p>
<%= label_tag 'settings[distancia_dias]', l(:label_setting_distancia_dias) %>
<%= text_field_tag('settings[distancia_dias]', @settings["distancia_dias"] || defaults["distancia_dias"]) %>
</p>
